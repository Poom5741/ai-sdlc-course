/**
 * Implement portPrompt.
 *
 * TODO: Implement the function below based on the contract in README.md
 * and the expectations in test.js. Run `node test.js` to verify.
 */

// portPrompt — replace the TODO body with your implementation
function portPrompt(prompt, fromModel, toModel) {
  throw new Error("TODO: implement portPrompt");
}

module.exports = { portPrompt };
