/**
 * Implement createPromptEvaluator.
 *
 * TODO: Implement the function below based on the contract in README.md
 * and the expectations in test.js. Run `node test.js` to verify.
 */

// createPromptEvaluator — replace the TODO body with your implementation
function createPromptEvaluator(testCases) {
  throw new Error("TODO: implement createPromptEvaluator");
}

module.exports = { createPromptEvaluator };
