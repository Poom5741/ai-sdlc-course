/**
 * Implement createChain.
 *
 * TODO: Implement the function below based on the contract in README.md
 * and the expectations in test.js. Run `node test.js` to verify.
 */

// createChain — replace the TODO body with your implementation
function createChain(steps) {
  throw new Error("TODO: implement createChain");
}

module.exports = { createChain };
