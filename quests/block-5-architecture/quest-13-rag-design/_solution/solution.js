/**
 * Quest 5.1: REFERENCE illustration (do NOT copy this into your rag-design.md)
 *
 * The learner writes their OWN rag-design.md for their RAG system over the
 * workshop docs. This file only documents the structure that test.js
 * validates.
 *
 * Required in rag-design.md:
 *   1. chunking strategy  (chunk)
 *   2. embedding strategy (embed)
 *   3. retrieval quality metric (metric | recall | precision | NDCG)
 *   4. failure handling (failure mode | fallback | graceful degradation)
 *   5. corpus identified  (corpus | workshop docs)
 *   6. >= 400 characters of substance
 *
 * See _solution/rag-design.md for a reference shape.
 */

module.exports = {};