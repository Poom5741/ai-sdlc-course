/**
 * Quest 5.2: REFERENCE illustration (do NOT copy this into your system-design.md)
 *
 * The learner writes their OWN system-design.md. This file only documents
 * the structure that test.js validates.
 *
 * Required in system-design.md:
 *   1. component diagram  (fenced ``` block with "→" | "-->" | "graph")
 *   2. interface contracts ("interface" | "contract" | "API")
 *   3. data flow           ("data flow" | "dataflow" | "flow:")
 *   4. named components     (>= 3 distinct component names)
 *   5. >= 500 characters of substance
 *
 * See _solution/system-design.md for a reference shape (automated PR reviewer).
 */

module.exports = {};