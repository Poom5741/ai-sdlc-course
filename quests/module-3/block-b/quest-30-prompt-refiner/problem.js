/**
 * Quest 3.8: Prompt-for-Code Refiner — problem.js (learner edits this)
 *
 * Block: 3 - Development with AI | Difficulty: 🟢 Easy | Time: 15 minutes
 *
 * Tool skill: refine prompts to generate better code.
 * Engineering habit: ITERATE ON PROMPTS — first drafts are rarely optimal;
 * refine prompts based on output quality.
 *
 * Goal: write `refinePrompt(badPrompt)` that improves a vague prompt.
 *
 * Return: string — improved prompt with specifics
 */
function refinePrompt(badPrompt) {
  return badPrompt;
}
module.exports = { refinePrompt };
