/**
 * Quest 3.9: Context Window Manager — problem.js (learner edits this)
 *
 * Block: 3 - Development with AI | Difficulty: 🟡 Medium | Time: 25 minutes
 *
 * Tool skill: manage context window by prioritizing relevant files.
 * Engineering habit: PRIORITIZE CONTEXT — not all files are equally relevant;
 * prioritize based on relevance to the task.
 *
 * Goal: write `prioritizeFiles(files, task, maxTokens)` that selects the
 * most relevant files within a token budget.
 *
 * Return: string[] — selected filenames in priority order
 */
function prioritizeFiles(files, task, maxTokens) {
  return [];
}
module.exports = { prioritizeFiles };
