/**
 * Quest 3.12: ReAct Pattern Agent — problem.js (learner edits this)
 *
 * Block: 3 - Development with AI | Difficulty: 🔴 Hard | Time: 35 minutes
 *
 * Tool skill: implement ReAct (Reason + Act) agent pattern.
 * Engineering habit: OBSERVE-PLAN-ACT — agents should reason about what
 * to do before acting, and observe results before reasoning again.
 *
 * Goal: write `reactAgent(goal, tools)` that implements a single ReAct cycle.
 *
 * Return: { observation, reasoning, action, result }
 */
function reactAgent(goal, tools) {
  return { observation: '', reasoning: '', action: null, result: null };
}
module.exports = { reactAgent };
