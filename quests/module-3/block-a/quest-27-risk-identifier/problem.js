/**
 * Quest 3.5: Risk Identifier — problem.js (learner edits this)
 *
 * Block: 3 - Planning & Requirements | Difficulty: 🟡 Medium | Time: 20 minutes
 *
 * Tool skill: identify project risks from description.
 * Engineering habit: ANTICIPATE FAILURES — identify risks early to mitigate them.
 *
 * Goal: write `identifyRisks(projectDescription)` that finds potential risks.
 *
 * Return: Array<{ risk: string, severity: 'low'|'medium'|'high', mitigation: string }>
 */
function identifyRisks(description) {
  return [];
}
module.exports = { identifyRisks };
