/**
 * Quest 3.1: User Story Generator — problem.js (learner edits this)
 *
 * Block: 3 - Planning & Requirements | Difficulty: 🟢 Easy | Time: 20 minutes
 *
 * Tool skill: generate structured user stories from raw notes.
 * Engineering habit: STRUCTURE REQUIREMENTS — user stories follow a format
 * that makes them testable and actionable.
 *
 * Goal: write `generateUserStory(notes)` that creates a user story from notes.
 *
 * Return: { asA: string, iWant: string, soThat: string, acceptanceCriteria: string[] }
 *
 * Format: "As a [role], I want [feature], so that [benefit]"
 *
 * Edge case: naive AI returns the raw notes instead of structured format.
 */

// TODO: implement generateUserStory(notes).
function generateUserStory(notes) {
  return { asA: '', iWant: '', soThat: '', acceptanceCriteria: [] };
}

module.exports = { generateUserStory };
