/**
 * Quest 3.3: ADR Generator — problem.js (learner edits this)
 *
 * Block: 3 - Planning & Requirements | Difficulty: 🟡 Medium | Time: 25 minutes
 *
 * Tool skill: generate Architecture Decision Records.
 * Engineering habit: DOCUMENT DECISIONS — ADRs capture why choices were made.
 *
 * Goal: write `generateADR(title, context, decision, consequences)` that
 * formats an ADR in standard markdown.
 *
 * Edge case: naive AI omits consequences section.
 */
function generateADR(title, context, decision, consequences) {
  return '';
}
module.exports = { generateADR };
