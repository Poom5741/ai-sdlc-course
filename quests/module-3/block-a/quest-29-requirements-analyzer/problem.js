/**
 * Quest 3.7: Requirements Quality Analyzer — problem.js (learner edits this)
 *
 * Block: 3 - Planning & Requirements | Difficulty: 🔴 Hard | Time: 30 minutes
 *
 * Tool skill: analyze requirements for quality issues.
 * Engineering habit: VALIDATE REQUIREMENTS — poor requirements lead to
 * building the wrong thing; analyze before implementing.
 *
 * Goal: write `analyzeRequirements(requirements)` that checks quality.
 *
 * Return: { score: number, issues: string[], suggestions: string[] }
 *   - score: 0-100 quality score
 *   - issues: list of problems found
 *   - suggestions: list of improvements
 */
function analyzeRequirements(requirements) {
  return { score: 0, issues: [], suggestions: [] };
}
module.exports = { analyzeRequirements };
