/**
 * Implement createFeatureFlag and evaluateFlag.
 *
 * TODO: Implement the functions below based on the contract in README.md
 * and the expectations in test.js. Run `node test.js` to verify.
 */

// createFeatureFlag(name, config) — replace the TODO body with your implementation
function createFeatureFlag(name, config = {}) {
  throw new Error("TODO: implement createFeatureFlag");
}

// evaluateFlag(flag, context) — replace the TODO body with your implementation
function evaluateFlag(flag, context) {
  throw new Error("TODO: implement evaluateFlag");
}

module.exports = { createFeatureFlag, evaluateFlag };
